[pytest]
filterwarnings =
    # Ignore the specific "coroutine was never awaited" warning that comes from
    # our sync-calls-async pattern for broadcasting WebSocket updates.
    # The tests pass and the application logic is correct.
    ignore:coroutine .* was never awaited:RuntimeWarning